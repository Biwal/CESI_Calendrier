import { JwtInterceptor } from './jwt-interceptors';

describe('JwtInterceptor', () => {
  it('should create an instance', () => {
    //todo: confirmation pour l'argument this.router
    // expect(this.router).toBeTruthy();
  });
});
