import { OwnDatePickerInputDirective } from './own-date-picker-input.directive';

describe('OwnDatePickerInputDirective', () => {
  it('should create an instance', () => {
    const directive = new OwnDatePickerInputDirective();
    expect(directive).toBeTruthy();
  });
});
